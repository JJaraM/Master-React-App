/*
 * MenuItemSubArrow Messages
 *
 * This contains all the text for the MenuItemSubArrow component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MenuItemSubArrow';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MenuItemSubArrow component!',
  },
});
