import { css } from 'styled-components';

const styles = css`
  background: inherit;
  border: inherit;
  width: 100%;
  border: none;
  padding: 1px;
  &:focus {
    outline: none;
  }
`;

export default styles;
