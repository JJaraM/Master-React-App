/*
 * SideBarSearchInput Messages
 *
 * This contains all the text for the SideBarSearchInput component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SideBarSearchInput';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SideBarSearchInput component!',
  },
});
