/*
 * SideMenuList Messages
 *
 * This contains all the text for the SideMenuList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SideMenuList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SideMenuList component!',
  },
});
