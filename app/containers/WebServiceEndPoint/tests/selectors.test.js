// import { selectWebServiceEndPointDomain } from '../selectors';

describe('selectWebServiceEndPointDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
