/*
 * WebServiceEndPoint Messages
 *
 * This contains all the text for the WebServiceEndPoint container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WebServiceEndPoint';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WebServiceEndPoint container!',
  },
});
