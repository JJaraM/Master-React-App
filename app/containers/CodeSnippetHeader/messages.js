/*
 * CodeSnippetHeader Messages
 *
 * This contains all the text for the CodeSnippetHeader container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CodeSnippetHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Add a new code snippet description',
  },
});
