// import { selectCodeSnippetHeaderDomain } from '../selectors';

describe('selectCodeSnippetHeaderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
