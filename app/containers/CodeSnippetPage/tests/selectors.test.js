// import { selectCodeSnippetPageDomain } from '../selectors';

describe('selectCodeSnippetPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
