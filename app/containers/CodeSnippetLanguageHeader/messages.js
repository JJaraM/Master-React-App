/*
 * CodeSnippetLanguageHeader Messages
 *
 * This contains all the text for the CodeSnippetLanguageHeader container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CodeSnippetLanguageHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Add New Language',
  },
});
