// import { selectCodeSnippetTypesDomain } from '../selectors';

describe('selectCodeSnippetTypesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
